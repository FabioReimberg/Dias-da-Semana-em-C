// bibliotecas
#include<stdio.h>
#include <conio.h>

// função Main e Variáveis Globais
int main() {
    int pergunta;

// Pergunta
    printf("insira um número inteiro positivo: ");
    scanf("%d", &pergunta);

// Resposta Correta
    while(pergunta >= 1) {
    printf("Obrigado!");
    }

// Definir loop 1
    while(pergunta < 0) {
    printf("insira um número inteiro positivo: ");
    scanf("%d", &pergunta);
    }




  return 0;
}
  
  
